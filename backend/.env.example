# Copy to `.env` and adjust as needed for local development.

APP_NAME="AI Incident Investigator"
APP_ENV=local
LOG_LEVEL=INFO
API_V1_PREFIX=/api/v1
DATABASE_URL=sqlite+aiosqlite:///./data/incident_investigator.db
